name: mf_env
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pandas
  - numpy
  - scipy
  - scikit-learn
  - matplotlib
  - seaborn
  - pip
  - pip:
      - streamlit
      - jupyter
      - notebook
      - tqdm
